{% extends "layout.jinja2" %}

{% block content %}
    <div class="row">
        <div class="col-3"></div>
        <div class="col-6">
            <div class="card mx-auto" style="margin-top: 10%;" >
                <div class="card-body">
                    <h3 class="card-title text-center">Register for Work Hub</h3>
                    
                    {% for message in get_flashed_messages() %}
                        <div class="alert alert-warning">
                            <button type="button" class="close" data-dismiss="alert">x</button>
                            {{ message }}
                        </div>
                    {% endfor %}
                    
                    <form method="POST" action="/register">
                        {{ form.csrf_token }}

                        <fieldset class="form-group row">
                            {{ form.role.label(class="col-form-label col-sm-2 float-sm-left pt-0") }}
                            {% for subrole in form.role %}
                                <div class="form-check form-check-inline">
                                    {{ subrole(class="form-check-input") }} {{ subrole.label(class="form-check-label orange") }}
                                </div>
                            {% endfor %}
                        </fieldset>
                        <div class="form-group">
                            {{ form.fname.label }} {{ form.fname(class="form-control", id="firstName", placeholder="Enter your first name") }}
                        </div>
                        <div class="form-group">
                            {{ form.lname.label }} {{ form.lname(class="form-control", id="lastName", placeholder="Enter your last name") }}
                        </div>
                        <div class="form-group">
                            {{ form.email.label }} {{ form.email(class="form-control", id="email", placeholder="Enter your Email Address") }}
                        </div>
                        <div class="form-group">
                            {{ form.contact.label }} {{ form.contact(class="form-control", id="phone", placeholder="Enter your Phone Number") }}
                        </div>
                        <div class="form-group">
                            {{ form.password.label }} {{ form.password(class="form-control", id="password", placeholder="Enter your password") }}
                        </div>
                        <div class="form-group">
                            {{ form.confirm.label }} {{ form.confirm(class="form-control", id="conPassword", placeholder="Enter your password again") }}
                        </div>
                        <!-- <div class="form-group">
                            <label for="profilePhoto">Profile photo</label>
                            <input type="file" class="form-control-file" id="profilePhoto">
                        </div> -->
                        {{ form.submit(class="btn btn-lg btn-block", style="background-color:#F07217; color:#ffffff; border-radius:20px;") }}
                    </form>
                </div>
            </div>
        </div>
    </div>
{% endblock content %}